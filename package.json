{
  "name": "charter-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site for Charter. The application itself lives in src/, and the SPA has its own package.json in src/Charter/ClientApp.",
  "//": "type: module is required for VitePress to load docs/.vitepress/config.ts. It applies only to this directory - src/Charter/ClientApp declares its own package.json and is unaffected.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/binn/Charter.git"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
